# Fresta Finance — Python dependencies

# Core data
pandas>=1.5.0
numpy>=1.24.0

# Market data
yfinance>=0.2.0

# HTTP (LM Studio / Anthropic API)
requests>=2.28.0

# Optional: Anthropic API (orders 4 & 5 via Claude)
anthropic>=0.20.0
